# Personal layer — default

For personal / non-work repos. Applied on top of any repo-local `CLAUDE.md`.

No additions beyond the global `~/.claude/CLAUDE.md` and the appended system
prompt. This file exists as the explicit default profile and a place to hang
personal-repo-specific guidance if it ever comes up.
